import "./InfoDialog.css"

function InfoDialog({arg_infoShowing, arg_setInfoShowing})
{
    return arg_infoShowing && (
        <div className="info-dialog">

        </div>
    );
}
export default InfoDialog;